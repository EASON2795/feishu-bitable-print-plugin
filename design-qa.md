# Design QA — 主工具栏导出菜单

## 目标

参考飞书原版工具栏，在现有产品设计系统中实现 `更多｜编辑｜导出｜打印`，其中“打印”保持唯一蓝色主操作，“导出”提供 PDF 与长图两个格式。

## 对照素材

- 参考：用户在本次任务中提供的飞书原版打印工具栏截图。
- 实现：`src/App.tsx`、`src/App.css` 与 `src/localExport.ts`。
- 浏览器测试视口：桌面、480px 与 360px。
- 状态：均为单据预览主界面；实现测试额外展开“导出”菜单以验证两个格式入口。

## 重点表面

| 表面 | 参考 | 实现 | 结果 |
| --- | --- | --- | --- |
| 主操作顺序 | 编辑、导出、打印 | 更多、编辑、导出、打印 | 通过；保留现有“更多”作为次级操作入口 |
| 层级 | 打印为蓝色主按钮 | 打印为唯一蓝色主按钮 | 通过 |
| 导出入口 | 白色次按钮 | 白色次按钮，菜单含 PDF/长图 | 通过 |
| 排版预览 | A4 单据居中 | A4 单据居中 | 通过 |
| 窄窗口 | 未提供 | 480px 四按钮等分，菜单不裁切 | 通过 |
| 键盘 | 未展示 | 上下方向键、Home/End、Escape 与焦点恢复 | 通过 |

## 迭代记录

1. 删除与“更多 → 收起模板栏”重复的顶部“隐藏模板”按钮，给“导出”留出稳定位置。
2. “更多”菜单改为向右展开，“导出”菜单向左对齐触发器，避免 360–480px 窄栏裁切。
3. 两个菜单统一补充 `aria-haspopup`、`aria-expanded`、`role=menu/menuitem`、方向键导航和 Escape 焦点恢复。
4. 导出菜单采用现有按钮、边框、圆角和阴影变量，没有引入与产品不一致的新视觉语言。

## 文件输出 QA

- 浏览器示例数据已分别完成 PDF 与 PNG 长图导出。
- PDF 与长图均完整保留示例版式、表格边框、文本内容和页边距；没有工具栏或编辑标记进入文件。
- PDF 已重新渲染检查页面完整性；长图已检查尺寸与视觉内容。

## 最终结果

**Passed** — 桌面与 480px 主流程、菜单位置、按钮层级、键盘路径、PDF 和长图文件均通过本地验证。
