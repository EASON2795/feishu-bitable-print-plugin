# 参与贡献

欢迎提交 Issue 和 Pull Request。

1. Fork 仓库并从 `main` 创建分支。
2. 不要提交真实客户、订单、银行、印章、手机号、邮箱、访问令牌或公司内部路径；示例必须完全虚构。
3. 运行 `npm ci`、`npm run lint` 和 `npm run build`。
4. 在 Pull Request 中说明行为变化、测试方式和隐私影响。

涉及数据外传、后端服务、身份认证或新增第三方请求的改动，需要先在 Issue 中讨论并补充威胁模型和隐私说明。
