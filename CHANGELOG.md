# 更新日志

## 0.7.0 - 2026-08-08

- 在主工具栏的“编辑”和“打印”之间新增“导出”菜单，提供“导出 PDF”和“导出长图”。
- “打印”只负责打开系统打印对话框，不再把 PDF 导出混在打印文案里。
- PDF 与 PNG 长图均在当前浏览器中生成并直接下载，不增加业务服务器或上传单据正文。
- PDF 按原模板页面逐页生成；长图会在浏览器安全尺寸内保持清晰，内容过长时提示改用 PDF。
- 外部图片无法被浏览器跨域读取时停止导出并给出明确提示，避免生成缺图文件。
- 同步优化“更多”和“导出”菜单的键盘操作、窄窗口定位和焦点恢复。

## 0.6.2 - 2026-08-08

- 将“导入旧模板”移到左侧模板栏底部，与“新建空白模板”集中展示，首次使用时更容易找到。
- 左上角搜索区只保留模板搜索；模板管理页内的导入入口继续保留，收起左栏后仍可操作。
- 恢复旧版的一键打印体验：点击“打印”后直接打开系统打印对话框，不再进入需要二次操作的独立打印工作台。

## 0.6.1 - 2026-08-08

- 修复旧版排版模板在数据源未配置完整时保存，会永久丢失 PDF 版式标识的问题。
- 已经受影响的本地模板会自动恢复，无需删除或重新导入；模板正文、图片、字段映射和排版设置保持不变。
- 导入模板时优先识别当前打开的主表，并通过关联字段自动找到实际连接的明细表；无法确认时再提示用户手动选择。
- PDF 排版样式与数据源就绪状态分开保存：未完成的模板可安全保存为草稿，补齐后可恢复为可打印模板。
- 优化“正在读取、当前表不匹配、尚未选择记录”的状态优先级，避免把加载中或表不匹配误显示为勾选 0 条。

## 0.6.0 - 2026-08-08

- 内置模板精简为一个“测试模板（形式发票）”，正式版式改为由用户导入自己的旧版模板文件。
- 左侧模板栏拆分为“我的模板”和“系统示例”，模板管理页增加首次导入引导。
- 导入按钮改为键盘可操作的真实按钮，并在原位置显示读取中、成功或失败结果。
- 旧版商业发票、装箱单或采购单若仍是活动模板，升级后会暂停打印并要求用户明确选择测试模板或导入旧模板，避免错用版式。
- 模板导入增加格式、空文件、2 MB 大小、页面结构和嵌套深度校验；无效 JSON 不再被误当作测试模板导入。
- 保留旧商业发票与装箱单渲染兼容，已有自定义模板和已导入模板不会因内置列表精简而失效。

## 0.5.1 - 2026-08-07

- 自定义模板的删除入口同时显示在左侧模板列表和模板管理卡片中。
- 新增清晰的删除确认窗口，并在删除后显示结果与运行记录。
- 系统内置模板明确标记为受保护，避免误删；可复制成自定义模板后修改。
- 删除当前模板后自动切回可用的内置模板，并重新读取当前勾选数据。
- 优化窄侧栏中的模板操作按钮尺寸和排列。

## 0.5.0 - 2026-08-07

- 产品主线回到飞书多维表格插件，Chrome 扩展调整为历史实验功能。
- 新增可拉伸的独立打印窗口，窗口内可反复检查单据、打印或另存为 PDF。
- 独立窗口只保存本次打印快照，关闭窗口即清除，不上传单据正文。
- 主界面合并重复的打印/PDF按钮，统一为“在新窗口打印”。
- 顶部直接显示当前表、模板所需表、勾选记录数、单据数和明细数。
- 模板入口改为“导入飞书模板”，明确支持 .txt 和兼容的 .json 文件。
- 诊断面板改为历史运行记录，旧错误不再被误报为当前仍有故障。

## 0.4.3 - 2026-08-07

- Chrome 主动同步时，飞书端会重新读取当前勾选，不再把旧快照当作本次结果。
- 同一请求重复到达时只执行一次飞书取数，避免重复读取与重复发布。

## 0.4.2 - 2026-08-07

- 主动补发的数据带有请求编号，Chrome 只接收本次请求对应的结果，避免多个窗口或迟到消息覆盖当前单据。

## 0.4.1 - 2026-08-07

- 飞书插件可响应 Chrome 右侧栏的主动数据请求，并立即补发当前勾选记录与模板。
- 修复部分飞书宿主不支持选择变化事件时，轮询兜底没有启动的问题。

## 0.4.0 - 2026-08-07

- 点击 Chrome 工具栏图标后，工作台改为在浏览器原生侧边面板中打开，不再新建标签页。
- 支持拖动侧边面板边界调整宽度；窄面板自动收起模板栏，拖宽后自动恢复。
- 优化窄面板中的工具栏、状态信息、预览区和模板管理布局。

## 0.3.1 - 2026-08-06

- 修复飞书自定义/官方版式同步到 Chrome 后字段和关联明细变空的问题。
- 同步消息现在同时携带当前排版模板；Chrome 优先按飞书侧模板预览和打印。
- 增加飞书网格勾选轮询，复选框变化后无需手动刷新插件即可重新同步。
- 补齐普通模板兼容字段，旧版 Chrome 或缺少模板时仍可正确显示单号、客户、金额和明细。
- 将大型多维表格的字段结构读取等待时间提高到 12 秒。

## 0.3.0 - 2026-08-06

- 增加“飞书插件官方 SDK 取数 → Chrome 打印台接收”的本地桥接。
- Chrome 打印台可以载入飞书插件读取到的当前勾选记录及关联明细。
- 同步数据临时保存在 Chrome 当前浏览器会话，关闭浏览器后清除。
- 权限仅增加扩展会话存储，以及精确的公开插件页面地址；不读取飞书 Cookie、浏览历史或剪贴板。
- 保留 CSV、TSV、JSON 本地导入作为备用入口。

## 0.2.0 - 2026-08-06

- 增加完全本地运行的 Chrome Manifest V3 试用版。
- 支持导入 CSV、TSV 和 JSON 单据数据，最多一次 20 张单据。
- Chrome 版不申请网页读取权限，不包含飞书 SDK，也不需要服务器。
- 点击扩展图标打开完整排版工作台，继续使用浏览器打印和“另存为 PDF”。
- 增加示例数据和 Chrome 安装、更新、数据格式说明。

## 0.1.0 - 2026-08-06

- 发布脱敏后的社区公开版。
- 支持飞书多维表格主记录与关联明细的单据排版。
- 支持形式发票、商业发票、装箱单模板入口与可视化模板编辑。
- 使用浏览器本地打印和“另存为 PDF”，不再依赖公共 PDF 后端。
- 自定义模板仅保存在当前浏览器。
- 增加隐私政策、使用条款、安全政策和 GitHub Pages 自动部署。
